/contact/*        200!   Role=admin
/contact/* /values 401    # fallback to the login page for unauthorized users

/admin/*        200!   Role=admin
/admin/* /values 401    # fallback to the login page for unauthorized users
